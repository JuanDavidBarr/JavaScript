/* Declara dos variables que se sumen (por ejemplo, cantidad de amigos en dos
redes sociales) y una que contenga el resultado de una concatenación. */
let friendsFacebook = 200;
let friendsInstagram = 500;
let totalFriends = friendsFacebook + friendsInstagram;
console.log(totalFriends)

let totalFriends2 = `I have ${friendsFacebook} friends on facebook and ${friendsInstagram} friends on instagram`;
console.log(totalFriends2);